<div class="max-w-7xl mx-auto py-8 px-4 sm:px-6 lg:px-8">
  <h1 class="text-2xl font-bold mb-4">Your Patients</h1>

  <div class="space-y-4">
    <% @patients.each do |patient| %>
      <%= render "doctors/patient", patient: patient %>
    <% end %>
  </div>
</div>
